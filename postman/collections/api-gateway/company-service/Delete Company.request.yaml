$kind: http-request
name: Delete Company
method: DELETE
url: "{{apiGatewayBaseUrl}}/api/companies/{{companyId}}"
order: 4433186086537018
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
