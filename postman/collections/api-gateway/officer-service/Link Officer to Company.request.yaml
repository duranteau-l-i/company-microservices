$kind: http-request
name: Link Officer to Company
method: POST
url: "{{apiGatewayBaseUrl}}/api/officers/{{officerId}}/links"
order: 4500030889285896
headers:
  - key: Content-Type
    value: application/json
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
body:
  type: json
  content: |-
    {
      "companyId": "{{companyId}}",
      "companyOwnerId": "{{userId}}",
      "title": "Secretary",
      "appointmentDate": "2024-06-01"
    }
