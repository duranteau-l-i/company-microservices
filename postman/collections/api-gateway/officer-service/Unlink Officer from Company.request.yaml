$kind: http-request
name: Unlink Officer from Company
method: DELETE
url: "{{apiGatewayBaseUrl}}/api/officers/{{officerId}}/links/{{companyId}}"
order: 4502010334288450
auth:
  credentials:
    token: "{{accessToken}}"
  type: bearer
