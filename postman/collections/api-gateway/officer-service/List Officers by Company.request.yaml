$kind: http-request
name: List Officers by Company
method: GET
url: "{{apiGatewayBaseUrl}}/api/officers/by-company/{{companyId}}"
order: 4503501058871332
auth:
  credentials:
    token: "{{accessToken}}"
  type: bearer
