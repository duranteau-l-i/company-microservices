$kind: http-request
name: Get Company
method: GET
url: "{{apiGatewayBaseUrl}}/api/companies/{{companyId}}"
order: 1018054202115666
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
