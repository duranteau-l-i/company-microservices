$kind: http-request
name: List Companies by Officer
method: GET
url: "{{apiGatewayBaseUrl}}/api/officers/{{officerId}}/companies"
order: 4503586686211300
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
