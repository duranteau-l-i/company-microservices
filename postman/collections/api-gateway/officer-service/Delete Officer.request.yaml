$kind: http-request
name: Delete Officer
method: DELETE
url: "{{apiGatewayBaseUrl}}/api/officers/{{officerId}}"
order: 4501105926753920
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
