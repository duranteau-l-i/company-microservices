server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/company_db
    username: ${POSTGRES_USER:admin}
    password: ${POSTGRES_PASSWORD:local-dev-password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
  data:
    mongodb:
      uri: mongodb://${MONGO_INITDB_ROOT_USERNAME:admin}:${MONGO_INITDB_ROOT_PASSWORD:local-dev-password}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/company_query_db?authSource=admin
  kafka:
    consumer:
      group-id: company-service-group

app:
  kafka:
    topics:
      company-events: company-events
      officer-events: officer-events
