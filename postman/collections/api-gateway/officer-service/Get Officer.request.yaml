$kind: http-request
name: Get Officer
method: GET
url: "{{apiGatewayBaseUrl}}/api/officers/{{officerId}}"
order: 3343941842406807
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
